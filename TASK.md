# 完整首轮任务

请从零实现“系统式分片纠删码库”，用于存储 / 有限域编码中的本地可复用模块。

1. 从零实现 GF(256) 分片纠删码，域多项式为 0x11d，加法为 XOR。参数 1<=k<=16，1<=m<=8，n=k+m；仅处理已知缺失分片，不承诺纠正未标记的坏字节。
2. 构造 n×k Vandermonde 矩阵 V，行 i 的点为字节 i+1，V[i,j]=(i+1)^j；生成矩阵 G=V*(V前k行的逆)，使前 k 行为单位阵。不得调用外部纠删码库。
3. Encode 将数据按顺序分为 k 个等长分片，末尾零填充，记录原始长度；空输入也合法。返回独立分片字节。恢复接受按位置索引的分片及缺失标记，从任意至少 k 个有效分片重建全部分片并裁剪原始数据。
4. 拒绝重复索引、非法索引、长度不一致、原始长度大于容量及不足 k 片；不要部分修改调用者输入。超过 k 片时恢复后核对全部已给分片，一致性不符返回错误；恰好 k 片时不宣称能检测静默损坏。
5. 域运算、矩阵求逆与分片操作分层；用小 k/m 穷举缺失组合和固定种子样本验证，增加有限域乘法的独立逐位参考实现。演示双分片丢失恢复及不足分片失败。

验收重点：空输入、非整除长度、丢数据片与校验片、任意 k 片、重复索引、额外片不一致、输入不被改写。

工程范围：这是全新核心库的0-1实现，初始仓库仅有运行骨架，没有业务实现。仅使用Go 1.26.5标准库，在Windows原生环境离线运行；无需安装第三方依赖。不接数据库、中间件、云API、外部程序，不使用Docker，不制作前后端或通用CLI。
交付：实现可复用库、聚焦上述语义的自动化测试和README；go run ./cmd/demo须在约8秒内运行确定性小案例，打印关键正常结果和至少一种真实边界失败，便于录制十几秒视频。测试命令为go test ./... -count=1 -timeout=60s。先阅读本仓库README和TASK.md，简短计划后集中实现，再运行针对性测试与演示；避免反复通读仓库、无关探索和无必要的长篇文档，发现实际问题时正常修复验证，不设硬性工具调用截断。
只能操作当前仓库内文件；不读取用户配置、密钥、环境变量清单或其他工作区。不要新增GitHub Actions、CI、部署、Dependabot或远程推送。一次首轮完成，不要求用户追加“继续”；合理实现取舍写入README。不要为通过测试删除需求、硬编码示例输出或宣称未实际执行的验证通过。
