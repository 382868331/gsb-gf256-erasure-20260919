# 完整首轮任务

离线归档模块需要在部分分片丢失时恢复原始对象。请从零实现“系统式分片纠删码库”。

1. 实现GF(256)已知擦除纠删码，域多项式0x11d，加法XOR。1<=k<=16，1<=m<=8，n=k+m；输入至多4MiB。仅恢复被明确标记缺失的分片，不承诺纠正静默损坏。
2. 构造n×k Vandermonde矩阵V，0起始行i取域元素i+1，V[i,j]=(i+1)^j；G=V乘以前k行方阵的逆，前k行须为单位阵。实现域运算、矩阵求逆和系统式编码，不调用第三方库。
3. 原长N决定每片长度s=ceil(N/k)，数据连续分为k片并在末尾补零，空输入s=0。恢复输入使用(index,bytes)列表，缺失索引不出现，零长bytes仍是有效分片；索引从0开始。
4. 从任意至少k片重建全部n片及原数据。拒绝重复/越界索引、负原长、片长不等于s、少于k片；检查恢复数据尾部补零并复核所有已给分片。错误不修改输入；恰好k片不一致性不可普遍检测，不虚假保证。
5. 输出字节与输入互不别名。对k<=3,m<=3穷举所有大小为k的可用索引组合，并用固定种子非整除长度样本验证；域乘法用独立逐位参考核对。演示双片丢失恢复及不足片失败。

验收重点：空输入有效空分片、非整除长度、任意k片、数据/校验片缺失、坏原长/片长、额外片不一致、输入不变。

工程约束：全新0-1核心库，仅用Go 1.26.5标准库，在Windows原生离线运行，无第三方依赖、外部服务、Docker或前后端。交付实现、聚焦上述边界的测试及简短README；演示命令`go run ./cmd/demo`用小案例在约8秒内打印正常结果和一种真实失败，测试命令`go test ./... -count=1 -timeout=60s`。固定种子随机验证只用少量小样本，避免长压测和额外平台功能，不限制正常纠错所需工具调用。
一次首轮完成，不要求用户追加“继续”。只操作当前仓库，不读取用户配置、密钥、环境变量清单或其他工作区；不添加Actions/CI/部署/Dependabot、不远程推送，不硬编码演示或虚报验证。
