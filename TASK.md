# 完整首轮任务

请实现一个分片纠删码库，用于离线文件分片后丢失部分分片的恢复。这里处理的是已知哪些分片丢失的情况；编码和恢复都需要从头实现。

实现GF(256)已知擦除纠删码，域多项式0x11d，加法XOR。1<=k<=16，1<=m<=8，n=k+m；输入至多4MiB。仅恢复被明确标记缺失的分片，不承诺纠正静默损坏。

构造n×k Vandermonde矩阵V，0起始行i取域元素i+1，V[i,j]=(i+1)^j；G=V乘以前k行方阵的逆，前k行须为单位阵。实现域运算、矩阵求逆和系统式编码，不调用第三方库。

原长N决定每片长度s=ceil(N/k)，数据连续分为k片并在末尾补零，空输入s=0。恢复输入使用(index,bytes)列表，缺失索引不出现，零长bytes仍是有效分片；索引从0开始。

从任意至少k片重建全部n片及原数据。拒绝重复/越界索引、负原长、片长不等于s、少于k片；检查恢复数据尾部补零并复核所有已给分片。错误不修改输入；恰好k片不一致性不可普遍检测，不虚假保证。

输出字节与输入互不别名。对k<=3,m<=3穷举所有大小为k的可用索引组合，并用固定种子非整除长度样本验证；域乘法用独立逐位参考核对。演示双片丢失恢复及不足片失败。

测试里请覆盖这些情况：空输入有效空分片、非整除长度、任意k片、数据/校验片缺失、坏原长/片长、额外片不一致、输入不变。

使用 Go 1.26.5 标准库，代码要能在 Windows 原生环境离线运行。范围就是这个库，不需要第三方依赖、外部服务、Docker 或前后端。请一并写好测试和简短的 README，说明接口及运行方法。

用 `go run ./cmd/demo` 运行演示，在约 8 秒内展示一个正常结果和一个实际触发的失败；用 `go test ./... -count=1 -timeout=60s` 运行测试。随机验证采用固定种子和少量小样本即可，不做长时间压力测试，也不扩展额外的平台功能。

请在本轮完成实现和验证，不要等我回复“继续”。正常排错需要的工具调用不设次数限制。操作范围限于当前仓库，不读取用户配置、密钥、环境变量清单或其他工作区。不要添加 Actions、CI、部署或 Dependabot，也不要向远端推送。演示结果应由代码实际计算；最后如实说明验证结果，未运行或未通过的检查也请写明。
